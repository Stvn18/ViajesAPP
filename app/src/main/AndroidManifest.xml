<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gt.umg.viajes">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MenuActivity"
            android:label="@string/title_activity_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ConfigurationActivity" />
        <activity android:name=".VueloActivity" />
        <activity android:name=".VueloDetalleActivity" />
        <activity android:name=".RegistroPasajeroActivity" />
        <activity android:name=".ResumenVueloActivity" />
        <activity android:name=".PagoVueloActivity" />
        <activity android:name=".CruceroActivity" />
        <activity android:name=".CruceroDetalleActivity" />
        <activity android:name=".CruceroInfoActivity" />
        <activity android:name=".CruceroResumenFacturacionActivity" />
        <activity android:name=".PagoCruceroActivity" />
        <activity android:name=".HotelActivity" />
        <activity android:name=".HotelSearchResultActivity" />
        <activity android:name=".FacturasActivity" />
        <activity android:name=".DetalleFacturaActivity"></activity>
    </application>

</manifest>